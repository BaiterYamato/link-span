{
  "schema_version": 1,
  "api_version": "0.4.0",
  "events": [
    {"name": "game.ready", "kind": "observe", "phase": "mvp", "cancellable": false, "support": ["oot", "mm"], "capability": null, "payload": [
      {"name": "game_id", "type": "game_id", "required": true},
      {"name": "host_version", "type": "string", "required": true},
      {"name": "runtime_version", "type": "string", "required": true},
      {"name": "api_version", "type": "string", "required": true}
    ]},
    {"name": "game.frame", "kind": "observe", "phase": "mvp", "cancellable": false, "support": ["oot", "mm"], "capability": null, "payload": [
      {"name": "frame", "type": "integer", "required": true}
    ]},
    {"name": "game.shutdown", "kind": "observe", "phase": "mvp", "cancellable": false, "support": ["oot", "mm"], "capability": null, "payload": []},
    {"name": "scene.enter", "kind": "observe", "phase": "host_bridge", "cancellable": false, "support": ["oot", "mm"], "capability": "scene.events", "payload": [
      {"name": "scene_id", "type": "integer", "required": true}
    ]},
    {"name": "actor.init", "kind": "observe", "phase": "host_bridge", "cancellable": false, "support": ["oot", "mm"], "capability": "actor.events", "payload": [
      {"name": "actor", "type": "actor_snapshot", "required": true}
    ]},
    {"name": "actor.update", "kind": "observe", "phase": "host_bridge", "cancellable": false, "support": ["oot", "mm"], "capability": "actor.events", "payload": [
      {"name": "actor", "type": "actor_snapshot", "required": true}
    ]},
    {"name": "actor.destroy", "kind": "observe", "phase": "host_bridge", "cancellable": false, "support": ["oot", "mm"], "capability": "actor.events", "payload": [
      {"name": "handle", "type": "actor_handle", "required": true}
    ]},
    {"name": "save.loaded", "kind": "observe", "phase": "host_bridge", "cancellable": false, "support": ["oot", "mm"], "capability": "save.events", "payload": [
      {"name": "slot", "type": "integer", "required": true}
    ]},
    {"name": "text.open", "kind": "observe", "phase": "host_bridge", "cancellable": false, "support": ["oot", "mm"], "capability": "text.events", "payload": [
      {"name": "text_id", "type": "integer", "required": true}
    ]},
    {"name": "audio.sequence_started", "kind": "observe", "phase": "host_bridge", "cancellable": false, "support": ["oot", "mm"], "capability": "audio.sequence.events", "payload": [
      {"name": "player_index", "type": "integer", "required": true},
      {"name": "sequence_id", "type": "integer", "required": true}
    ]},
    {"name": "input.hotkey", "kind": "observe", "phase": "host_bridge", "cancellable": false, "support": ["oot", "mm"], "capability": null, "payload": [
      {"name": "action", "type": "string", "required": true},
      {"name": "key", "type": "string", "required": true}
    ]},

    {"name": "hook.oot.player.speed.run", "kind": "transform", "phase": "hook_bridge", "cancellable": false, "support": ["oot"], "capability": "hooks.bridge", "payload": [
      {"name": "speed", "type": "number", "required": true}
    ]},
    {"name": "hook.oot.player.fall_damage", "kind": "transform", "phase": "hook_bridge", "cancellable": false, "support": ["oot"], "capability": "hooks.bridge", "payload": []},
    {"name": "hook.oot.item.receive", "kind": "observe", "phase": "hook_bridge", "cancellable": false, "support": ["oot"], "capability": "hooks.bridge", "payload": [
      {"name": "item_id", "type": "integer", "required": true},
      {"name": "get_item_id", "type": "integer", "required": true}
    ]},
    {"name": "hook.oot.player.health_change", "kind": "observe", "phase": "hook_bridge", "cancellable": false, "support": ["oot"], "capability": "hooks.bridge", "payload": [
      {"name": "amount", "type": "integer", "required": true}
    ]},
    {"name": "hook.oot.player.bonk", "kind": "observe", "phase": "hook_bridge", "cancellable": false, "support": ["oot"], "capability": "hooks.bridge", "payload": []},
    {"name": "hook.oot.enemy.defeat", "kind": "observe", "phase": "hook_bridge", "cancellable": false, "support": ["oot"], "capability": "hooks.bridge", "payload": [
      {"name": "actor_id", "type": "integer", "required": true},
      {"name": "category", "type": "integer", "required": true},
      {"name": "pos_x", "type": "number", "required": true},
      {"name": "pos_y", "type": "number", "required": true},
      {"name": "pos_z", "type": "number", "required": true}
    ]},
    {"name": "hook.oot.item.give", "kind": "transform", "phase": "hook_bridge", "cancellable": false, "support": ["oot"], "capability": "hooks.bridge", "payload": [
      {"name": "item_id", "type": "integer", "required": true},
      {"name": "get_item_id", "type": "integer", "required": true}
    ]},
    {"name": "hook.oot.hud.draw", "kind": "observe", "phase": "hook_bridge", "cancellable": false, "support": ["oot"], "capability": "hud.draw", "payload": []},
    {"name": "hook.oot.player.first_person_control", "kind": "observe", "phase": "hook_bridge", "cancellable": false, "support": ["oot"], "capability": "hooks.bridge", "payload": [
      {"name": "held_item_action", "type": "integer", "required": true}
    ]},
    {"name": "hook.oot.player.arrow_type_select", "kind": "transform", "phase": "hook_bridge", "cancellable": false, "support": ["oot"], "capability": "hooks.bridge", "payload": [
      {"name": "magic_arrow_type", "type": "integer", "required": true},
      {"name": "arrow_type", "type": "integer", "required": true}
    ]},
    {"name": "hook.oot.player.body_anim_select", "kind": "transform", "phase": "hook_bridge", "cancellable": false, "support": ["oot"], "capability": "oot.player.custom_body", "payload": [
      {"name": "speed", "type": "number", "required": true},
      {"name": "on_ground", "type": "boolean", "required": true},
      {"name": "rolling", "type": "boolean", "required": true},
      {"name": "roll_charge", "type": "integer", "required": true},
      {"name": "roll_phase", "type": "string", "required": true},
      {"name": "falling", "type": "boolean", "required": true},
      {"name": "landing", "type": "boolean", "required": true},
      {"name": "climbing", "type": "boolean", "required": true},
      {"name": "climb_direction", "type": "string", "required": true},
      {"name": "climb_step", "type": "integer", "required": true},
      {"name": "climb_starting", "type": "boolean", "required": true},
      {"name": "door_opening", "type": "boolean", "required": true},
      {"name": "door_direction", "type": "string", "required": true},
      {"name": "chest_opening", "type": "boolean", "required": true},
      {"name": "instrument", "type": "boolean", "required": true},
      {"name": "attacking", "type": "boolean", "required": true},
      {"name": "attack_animation", "type": "integer", "required": true}
    ]},

    {"name": "hook.mm.player.speed.walk", "kind": "transform", "phase": "hook_bridge", "cancellable": false, "support": ["mm"], "capability": "hooks.bridge", "payload": [
      {"name": "speed", "type": "number", "required": true}
    ]},
    {"name": "hook.mm.player.goron_roll.consume_magic", "kind": "transform", "phase": "hook_bridge", "cancellable": false, "support": ["mm"], "capability": "hooks.bridge", "payload": []},
    {"name": "hook.mm.player.goron_roll.disable_spike_mode", "kind": "transform", "phase": "hook_bridge", "cancellable": false, "support": ["mm"], "capability": "hooks.bridge", "payload": []},
    {"name": "hook.mm.player.goron_roll.increase_spike_level", "kind": "transform", "phase": "hook_bridge", "cancellable": false, "support": ["mm"], "capability": "hooks.bridge", "payload": []},
    {"name": "hook.mm.item.give", "kind": "observe", "phase": "hook_bridge", "cancellable": false, "support": ["mm"], "capability": "hooks.bridge", "payload": [
      {"name": "item", "type": "integer", "required": true}
    ]},
    {"name": "hook.mm.enemy.defeat", "kind": "observe", "phase": "hook_bridge", "cancellable": false, "support": ["mm"], "capability": "hooks.bridge", "payload": [
      {"name": "actor_id", "type": "integer", "required": true},
      {"name": "category", "type": "integer", "required": true},
      {"name": "pos_x", "type": "number", "required": true},
      {"name": "pos_y", "type": "number", "required": true},
      {"name": "pos_z", "type": "number", "required": true}
    ]},
    {"name": "hook.mm.item.should_give", "kind": "transform", "phase": "hook_bridge", "cancellable": false, "support": ["mm"], "capability": "hooks.bridge", "payload": [
      {"name": "item", "type": "integer", "required": true}
    ]}
  ]
}
